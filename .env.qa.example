# Copia a .env.qa y completa. Nunca commitear .env.qa real.

# Obligatorio para tests que CREAN data (gastos, vehículos QA)
QA_ALLOW_DB_WRITES=1

PLAYWRIGHT_BASE_URL=http://localhost:5173
PLAYWRIGHT_SKIP_WEB_SERVER=1

QA_USER_EMAIL=
QA_USER_PASSWORD=

CONTADOR_EMAIL=contador@lamoneda.com
CONTADOR_PASSWORD=lamoneda2026

# Mismas credenciales Supabase que usa la app (cleanup por API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_EMPRESA_ID=

# Módulo Garantías (Fase 1). Off por defecto.
# VITE_GUARANTEES_MODULE=1
# VITE_GUARANTEES_AUTO_ASSIGNMENT=1  # Fase 2 — no activar todavía

