/** Duración visible del toast de éxito (sin acción). */
export const TOAST_SUCCESS_MS = 1800;

/** Duración visible del toast de éxito con botón «Deshacer». */
export const TOAST_UNDO_MS = 2500;

/** Fallback cuando no se pasa `duration` explícita. */
export const TOAST_DEFAULT_MS = TOAST_SUCCESS_MS;

/** Errores: tiempo suficiente para leer el mensaje, sin bloquear la UI. */
export const TOAST_ERROR_MS = 2500;

export const TOAST_ENTER_MS = 140;
export const TOAST_EXIT_MS = 120;
